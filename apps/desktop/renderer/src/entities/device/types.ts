export type DesktopBootstrapPayload = {
  clientVersion: string;
  deviceName: string;
  deviceOs: string;
  password: string;
  publicKey: string;
  publicKeyFingerprint: string;
  username: string;
};
