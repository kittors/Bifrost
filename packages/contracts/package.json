{
  "name": "@bifrost/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "node ./scripts/generate-contracts.mjs --check && tsc --noEmit",
    "check": "node ./scripts/generate-contracts.mjs --check && tsc --noEmit",
    "generate": "node ./scripts/generate-contracts.mjs",
    "test": "vitest run src/*.test.ts"
  },
  "devDependencies": {
    "@bifrost/config-typescript": "workspace:*",
    "typescript": "6.0.3",
    "vitest": "4.1.4",
    "yaml": "2.8.1"
  }
}
